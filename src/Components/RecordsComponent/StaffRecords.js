import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class StaffRecords extends Component {
    // static propTypes = {
    //     prop: PropTypes
    // }

    render() {
        return (
            <div>
                Staff Records Sections
            </div>
        )
    }
}
